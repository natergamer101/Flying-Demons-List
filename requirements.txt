Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.0.5
Flask-WTF==1.2.1
WTForms==3.1.1
psycopg[binary]>=3.1.0
python-dotenv==1.0.0
email-validator==2.1.0

# For generating a static snapshot of the site (used for GitHub Pages)
Frozen-Flask==1.0.2

# WSGI server for production deployment/demo hosting
# (used by Heroku, Render, etc.)
# gunicorn 23.1.0 is not published; bump to a released version.
# Locking to 23.0.0 which is known to exist, or use a range if
# you prefer to track newer releases.
gunicorn==23.0.0
